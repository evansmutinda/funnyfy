{
  "name": "funnyfyapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Use 'npx vercel dev' from the project root instead of 'npm run dev'\" && exit 1"
  },
  "dependencies": {
    "@vercel/node": "^2.3.0",
    "@vercel/postgres": "^0.10.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.16.0",
    "typescript": "^5.3.3"
  }
}
